# Load library
library("shiny")
library("shinydashboard")

# Run examples
runGitHub(repo = "shiny-examples", username = "rstudio", subdir = "012-datatables")

runGitHub(repo = "shiny-examples", username = "rstudio", subdir = "018-datatable-options")

runGitHub(repo = "shiny-examples", username = "rstudio", subdir = "030-basic-datatable")

runGitHub(repo = "shinydashboard", username = "rstudio", ref = "gh-pages", subdir = "_apps/sidebar-tabs")

runGitHub(repo = "shiny-examples", username = "rstudio", subdir = "009-upload")

runGitHub(repo = "shiny-examples", username = "rstudio", subdir = "066-upload-file")

runGitHub(repo = "shiny-examples", username = "rstudio", subdir = "010-download")

runGitHub(repo = "shiny-examples", username = "rstudio", subdir = "039-download-file")

runGitHub(repo = "shiny-examples", username = "rstudio", subdir = "124-async-download")
